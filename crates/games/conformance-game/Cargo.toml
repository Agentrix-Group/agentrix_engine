[package]
name = "agentrix-conformance-game"
version = "0.1.0"
edition = "2021"

[dependencies]
agentrix-sim-core = { path = "../../sim-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
